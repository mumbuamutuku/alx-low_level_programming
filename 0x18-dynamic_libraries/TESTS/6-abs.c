#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 *
 * @h: - the integer to be tested
 *
 * Return: 0 always
 */
int _abs(int h)
{
	if (h >= 0)
		return (h);
	else
		return (-h);
}
